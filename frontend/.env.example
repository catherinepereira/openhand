# Set to "true" to show the speak button (requires backend ElevenLabs key)
VITE_TTS_ENABLED=false

# Optional: override the backend origin if not running on the default
# http://localhost:8273. Used by src/config.ts.
# VITE_API_BASE=http://localhost:8273
