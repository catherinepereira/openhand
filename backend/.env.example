# Copy to backend/.env and fill in keys you need.
# Everything in here is optional; the backend runs without an .env.

# ElevenLabs API key for text-to-speech output.
# Get one at https://elevenlabs.io/. Without it, POST /api/tts returns an
# error and the speak button stays hidden in the frontend.
ELEVENLABS_API_KEY=
